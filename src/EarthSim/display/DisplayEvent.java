package EarthSim.display;

import EarthSim.common.SimulationSettings;

public class DisplayEvent {
	SimulationSettings settings;
	public DisplayEvent(SimulationSettings settings) {
		this.settings = settings;
	}

}
